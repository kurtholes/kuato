# =============================================================================
# KUATO CONFIGURATION
# =============================================================================
# Copy this file to .env and configure before running:
#   cp .env.example .env
#   # Edit .env with secure values
#   docker-compose up -d
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------

# PostgreSQL credentials - CHANGE THESE!
# Generate a secure password: openssl rand -base64 32
POSTGRES_USER=claude
POSTGRES_PASSWORD=CHANGE_ME_TO_A_SECURE_PASSWORD
POSTGRES_DB=claude_sessions

# Database host binding (127.0.0.1 = localhost only, 0.0.0.0 = all interfaces)
# WARNING: Only use 0.0.0.0 if you understand the security implications
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5433

# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------

# API authentication key - REQUIRED
# Generate a secure key: openssl rand -base64 32
KUATO_API_KEY=CHANGE_ME_TO_A_SECURE_API_KEY

# API host binding (127.0.0.1 = localhost only)
API_HOST=127.0.0.1
API_PORT=3847

# Rate limiting (requests per minute)
KUATO_RATE_LIMIT=100

# -----------------------------------------------------------------------------
# SESSION DATA
# -----------------------------------------------------------------------------

# Path to Claude Code sessions directory
CLAUDE_SESSIONS_DIR=~/.claude/projects

# -----------------------------------------------------------------------------
# DIRECT API USAGE (without Docker)
# -----------------------------------------------------------------------------

# When running the API directly with bun, use this DATABASE_URL format:
# DATABASE_URL=postgres://claude:YOUR_PASSWORD@localhost:5433/claude_sessions
